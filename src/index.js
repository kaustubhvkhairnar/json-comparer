import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './App.css'; // Import CSS for wave animation

ReactDOM.createRoot(document.getElementById('root')).render(<App />);
